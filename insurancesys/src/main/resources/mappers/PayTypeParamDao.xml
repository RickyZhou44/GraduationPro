<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="whu.iss.insurancesys.dao.PolicyParamDaos.PayTypeParamDao">

    <insert id="insert"
            parameterType="whu.iss.insurancesys.entity.PolicyParamEntities.PayTypeParam"
            useGeneratedKeys="true"
            keyProperty="id">
        insert into pay_type_param(name, description) value (#{name}, #{description});
    </insert>

    <update id="update"
            parameterType="whu.iss.insurancesys.entity.PolicyParamEntities.PayTypeParam">
        update pay_type_param
        <set>
            <if test="name != null">name = #{name}, </if>
            <if test="description != null">description = #{description}, </if>
        </set>
        <where>
            <if test="id != null">id = #{id}</if>
        </where>
        ;
    </update>

    <delete id="delete"
            parameterType="whu.iss.insurancesys.entity.PolicyParamEntities.PayTypeParam">
        delete from pay_type_param where id = #{id};
    </delete>

    <select id="getAll"
            resultType="whu.iss.insurancesys.entity.PolicyParamEntities.PayTypeParam">
        select * from pay_type_param;
    </select>

</mapper>