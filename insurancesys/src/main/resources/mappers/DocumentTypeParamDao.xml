<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="whu.iss.insurancesys.dao.HumanResourceParamDaos.DocumentTypeParamDao">

    <update id="update"
            parameterType="whu.iss.insurancesys.entity.HumanResourceParamEntities.DocumentTypeParam">
        update document_type_param
        <set>
            <if test="id != null">id = #{id}, </if>
            <if test="name != null">name = #{name}, </if>
        </set>
        <where>
            <if test="id != null">id = #{id}</if>
        </where>
        ;
    </update>

    <select id="getAll"
            parameterType="whu.iss.insurancesys.entity.HumanResourceParamEntities.DocumentTypeParam">
        select * from document_type_param;
    </select>

</mapper>